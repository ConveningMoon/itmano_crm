export default function SettingsPage() {
  return (
    <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', height: '240px' }}>
      <p style={{ color: 'var(--text-muted)' }}>Configuración — contenido en construcción</p>
    </div>
  )
}
